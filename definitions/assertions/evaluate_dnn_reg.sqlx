config {
  type: 'assertion',
  schema: 'df_exp_assertions'
}

SELECT
  *
FROM
  ML.EVALUATE(
    MODEL ${ref('dnn_reg')}, 
    (
      SELECT * FROM ${ref('chicago_taxi_trips_evaluation')}
    )
  )
WHERE mean_absolute_error > 6